export default function OffresPage() {
  return (
    <main className="mx-auto flex w-full max-w-lg flex-1 flex-col px-5 pb-6 pt-[max(1.5rem,env(safe-area-inset-top))]">
      <h1 className="text-2xl font-semibold tracking-tight">Offres</h1>
      <p className="mt-2 text-muted-foreground">
        Les avantages et offres personnalisées seront branchés dans une étape
        ultérieure.
      </p>
    </main>
  );
}
